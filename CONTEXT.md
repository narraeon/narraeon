# Narraeon

Narraeon 是一个以提示词编排和本地权威连续性为核心的开放式角色扮演工作区；作者、玩家与 AI 负责世界语义，Runtime 负责安全操作、提交和恢复。

## Language

### 内容与世界

**内容包**：
尚未创建世界时可导入、编辑、复制和导出的一棵文件树；它是世界模板，不是正在游玩的世界。
_Avoid_：安装物、插件、存档

**内容包本地身份**：
Runtime 为一份本地内容包分配的稳定身份；同标题同内容的导入或复制仍获得彼此独立的身份。
_Avoid_：内容包标题、来源路径、内容摘要

**内容包标题**：
作者为一份本地内容包设置的可修改、可重名工作区标题；它独立保存在内容包外壳中，只帮助人和设定完善 AI 识别正在创作的模板，不属于内容包当前树、世界内事实或任一世界文档。创建世界时它只成为新世界的初始本地名称。
_Avoid_：当前情境标题、世界文档标题、内容包本地身份、世界名称

**内容包设定**：
内容包中全部虚构初始内容与控制资产的统称；“设定”只用于创建世界之前，复制后的同一虚构内容直接成为世界状态。
_Avoid_：世界设定副本、初始记录、运行中状态

**内容包当前树**：
一份内容包此刻唯一可编辑的完整文件集合；保存以整批候选替换当前内容，不形成包级历史版本。
_Avoid_：发布版本、content revision、manifest

**世界**：
从一份内容包复制后独立演化的虚构连续体；它拥有自己的状态、控制、历史和 Runtime 账本，不再与源内容包同步。
_Avoid_：内容包、模型上下文、聊天记录

**世界名称**：
玩家为一个世界设置的本地显示名称；它可以修改和重名，只帮助人在工作区中识别世界，不属于虚构状态，也不改变世界身份、当前端点或权威历史。
_Avoid_：当前情境标题、内容包标题、世界 ID

**世界状态**：
一个世界 `state` 表面中全部虚构文档的当前值；人物、地点、物品、规则和当前情境都在其中，不再另有“设定副本＋当前记录”双层。
_Avoid_：世界设定、状态 DTO、权威历史

**世界当前树**：
世界状态在当前端点的完整可读文件树；它是模型和玩家编辑器面对的当前表示，并可由不可变提交恢复。
_Avoid_：候选工作区、设定副本、第二份状态缓存

**世界控制**：
随世界保存、决定怎样主持、拼装和显示的控制资产，包括世界提示框架、创作块和玩家视图；它不保存虚构世界的实际当前值。
_Avoid_：世界状态、宿主预设、世界规则

**开场白**：
内容包中由作者预先写定、随世界创建成为第一条已提交主持原文的一段玩家可见叙事；它在玩家首次行动前已经成立，并停在可供玩家行动的局面。生产提示词不把这条 genesis 作为近期历史或附加材料重复注入。
_Avoid_：开局状态、默认玩家输入、世界提示词、模型临场生成的第一段回复

**当前情境**：
世界状态中一份由世界提示框架精确绑定的必需开放文档，用来承接没有单一自然所有者、却会约束紧接着叙事的短期局面；它不是整个世界状态的摘要。其文档标题与简介是随局面更新的当前场景索引，不需要保留“当前情境”字样，也不决定这份文档是否承担当前情境职责。
_Avoid_：内容包标题、场景 DTO、最近交互摘要、完整当前状态

**自然所有者**：
某项持续信息最自然依附的人物、地点、物品或其他世界文档；有自然所有者的结果应优先写回该文档，而不是重复堆进当前情境。
_Avoid_：Runtime 固定实体类型、schema 主体

**关键道具**：
其身份、归属、意义或变化会持续影响玩家选择的物件；只有需要独立引用、转移或追踪生命周期时才升级为独立世界文档。
_Avoid_：叙事里出现的每件物品、完整物理对象

**世界语义规则**：
以 Markdown 或自然语言字段解释修为、关系、好感度等世界含义的内容；它约束 AI 的判断，但不构成 Runtime 的枚举、范围或状态转换校验。
_Avoid_：世界数据 schema、机械契约、Runtime 领域规则

**设定完善**：
围绕内容包当前树进行的对话式 AI 创作过程；每段对话固定一份玩法预设和模型绑定作为语义与 Provider 上下文，AI 根据用户每条普通消息自行选择读取、讨论或直接修改内容包当前树。完整工具响应中的成功改动结算后立即成为当前树权威。
_Avoid_：计划阶段、生成阶段、隔离草稿、待应用候选、世界状态更新、普通游玩

**设定完善对话**：
一段 append-only 的作者消息、Provider 原生续传、返回推理、工具调用与工具结果历史；默认在当前所选对话上继续，作者也可以显式从内容包当前树开启全新上下文。全部历史对话都持久保留并可随时选中继续。
_Avoid_：当前草稿、只读归档、把旧对话嵌进新消息

**设定工具响应结算**：
一份完整 Provider 工具响应中的调用按顺序在同一当前树 revision 上执行，成功调用看得到此前成功结果，失败调用不回滚同级成功调用；响应完成后全部成功变化一次原子发布，并把逐调用差异、检查和真实 Prompt Preview 收据追加回对话。
_Avoid_：逐文件半发布、用户 Apply、从不完整流式片段执行工具

**设定改动回滚**：
作者从一条成功的历史工具回执中选择一份文件，把该文件直接恢复为记录中的修改前像；创建操作的前像为空，因此回滚该文件就是删除，删除操作则重新创建。每份文件独立选择，当前内容即使后来改变也由这次显式回滚覆盖。回滚不调用模型，也不删除或改写对话历史。
_Avoid_：回到整份内容包快照、强制捆绑同一调用的多份文件、删除审计记录、向模型发送反向修改请求

**设定改动游玩读取覆盖**：
从写入后真实 Prompt Preview 得出的确定性说明，区分一份改动材料在全新游玩中是全文／节点注入、仅有目录摘要、由已注入材料直接引用、只能按需发现，还是控制块根本未被启用；它只描述实际读取路径，不判断内容的重要性或充分性。
_Avoid_：相关度评分、可见性权限、语义检索结果

### 连续性与历史

**重要性分级连续性**：
按遗忘是否会让玩家出戏、是否会改变重要选择来决定保存和检索强度；越重要的事情越不能冲突，普通环境细节可以合理补全或遗忘。
_Avoid_：完整世界模拟、所有细节同等追踪

**叙述连贯性**：
连续若干段可见叙事在人称、语气、指代和已建立细节上成立为同一段连续叙述的程度；它与事实是否正确相互独立，事实全对但读起来像每次都重新认识玩家仍然不成立。
_Avoid_：文风预设、重要性分级连续性、模型记忆

**叙事细节**：
只存在于已提交玩家／主持原文、没有另行写入世界状态的信息；它可被精确追溯，但 Runtime 不把它自动提取成当前事实。
_Avoid_：结构化状态、自动摘要、模型记忆

**重要认知**：
忘记后会明显破坏某个人物行为连续性的知道、相信、怀疑或误解；需要持久时直接写入该人物文档，不要求固定认知 schema。
_Avoid_：世界上玩家不知道的一切、秘密分类、Runtime 认知记录类型

**已提交叙事**：
按 Authority 接受顺序不可改写地保存的开场白、玩家输入与最终可见主持原文；当前页面只投影当前时间线，被修改替换的旧原文仍可从旧端点恢复。它是一等连续性来源，但不是 Runtime 自动解释的当前状态。
_Avoid_：最近消息缓存、叙事摘要、模型会话记录

**权威历史**：
一个世界持有的世界中立 Authority 事实形成的不可改写 audit 总顺序；事实和历史消息使用 Authority 内局部身份，不嵌入 world 或 operation 身份。游玩、连续性修正和时间线修订都只追加新事实，创建分叉则保留旧端点的完整物理闭包，任何操作都不删除或覆盖已接受事实。
_Avoid_：聊天分支、可编辑消息、当前状态树

**当前端点**：
世界最近一次成功权威提交后的位置；V1 用 `commit:N` 作为世界内别名，并用不可变摘要与直接结果 root 确定持久内容。新的游玩、修正、修改和创建分叉都必须明确绑定一个端点。
_Avoid_：最新模型消息、文件修改时间

**当前时间线**：
当前端点实际恢复出的世界状态、已提交叙事、附加材料和模型调用轨迹；时间线修订可以让它从旧端点改道，但不会改写 Authority 中已经接受的记录。
_Avoid_：权威历史、模型会话、可变提交链

**页面时间线**：
当前世界面向玩家连续呈现的开场白、模型上下文分隔、玩家／AI 原文和调用轨迹；它是可重建的显示投影，诊断详情可以按需查看，不是世界权威或模型上下文本身。
_Avoid_：聊天记录、权威历史、模型 transcript

**时间线修订**：
玩家确认“修改”一条已提交玩家消息后，Runtime 在同一世界追加的一笔双父 Authority 事实；audit parent 保留接受总序，timeline parent 指向该玩家消息的逻辑父端点，直接结果 root 原子追加修改稿，使原消息及其后续离开当前时间线但继续留在旧端点中。玩家可以选择沿原模型上下文前缀继续，也可以把修改稿作为全新上下文的第一条消息；后一种选择只重开模型上下文，不重复提交修改稿。
_Avoid_：创建分叉、破坏性回滚、覆盖旧提交、连续性修正

**分叉世界**：
通过“创建分叉”保留旧端点 Authority 与页面／模型轨迹物理闭包而建立的新世界；不可变前缀逐字节相同，可变投影不共享写入，复制节点仍是可恢复、可继续分叉的本地端点。它使用创建分叉时的当前控制与主持选择，在同一 staging 原子发布后不依赖来源。
_Avoid_：派生世界、原世界回滚、继续同一 world ID、隐藏分支

**连续性修正**：
玩家在世界外明确授权、以新权威提交替换当前文档或附加材料清单的操作；它保留旧提交和旧叙事，不伪装成世界内事件。
_Avoid_：历史改写、剧情圆场、直接覆盖存档

**世界修订**：
玩家在故事外对运行中世界进行的一段持久创作过程；手动编辑与 AI 对话共用唯一的 `state`／`control` 修订工作树，只有“应用”才追加世界权威并发布控制，“放弃”则保持世界不变。已经成为 genesis 叙事的开场白没有可写的当前世界文件。
_Avoid_：内容包设定完善、普通游玩、并发草稿、直接覆盖世界文件

**世界修订 epoch**：
一次从固定当前端点和控制指纹开始、由 Runtime 持久独占锁保护的世界修订生命周期；浏览器关闭或 Runtime 重启都不让它过期，只有应用或放弃才封存工作树并解锁世界。活动期间游玩和其他世界语义写入均被拒绝，本地世界名称仍可修改。
_Avoid_：短期编辑会话、租约、可与游玩合并的分支、模型会话

**世界修订改动回滚**：
玩家从活动 epoch 的手动或 AI 历史改动中选择一份文件，把它恢复为该次改动记录的前像；当前内容即使后来改变也由这次显式回滚覆盖，并把反向变化继续追加到同一历史。应用或放弃后历史仍可查看，但不能再据此修改已封存工作树。
_Avoid_：整批接受或拒绝草稿、回退 Authority、删除对话历史、自动 merge

### 文档与引用

**世界文档**：
内容包 `world` 或世界 `state` 中一份受限 YAML／Markdown 文件；一份文档承载一个自然所有者、独立生命周期或独立长篇语义。
_Avoid_：七字段 JSON 对象、数据库记录、每字段一文件

**退役文档**：
仍完整保存在世界 `state`、可列出、搜索、读取、引用和恢复，但不再参与世界 frame 的 catalog 注入的文档；它保留已经发生的叙事记录，又不继续占用常驻目录索引预算。
_Avoid_：物理删除、隐藏历史、自动清理、软删除文件

**文档身份**：
Runtime 在文档技术存储中维护的世界内稳定 ID；显示名、路径、标题和短引用变化都不改变它，但模型提示、工具参数和工具结果从不暴露它。
_Avoid_：模型句柄、文件路径、显示名称、Runtime 内部版本

**文档短引用**：
AI 创建文档时给出的稳定、简短公开句柄；Runtime 校验唯一性并把它映射到文档身份，模型工具、预览、正文引用和控制选择器统一写成 `@短引用`，不暴露内部 ID、宿主路径、hash 或 operation 信息。
_Avoid_：文件名、临时 tool-call ID、显示名猜测

**文档引用**：
模型面对的世界内容用显式 `$ref: @短引用` 指向整份文档；Runtime 在技术存储中可保存内部文档身份，向模型读取时始终投影回当前 `@短引用`。裸字符串始终只是有意义的文字，Runtime 不猜测其中是否藏有 ID。
_Avoid_：内部 ID 引用、名称引用、字段名推断、自动语义关系

**逻辑节点**：
由一份文档加 YAML 路径或 Markdown 标题路径精确定位的可读写片段；一次工具 patch 的 YAML 路径可以用 map key 或数组下标精确删改，但长期保存在 frame 或 player-view 中的选择器应避免依赖易漂移的数组下标。它不是可脱离所属文档长期存在的独立对象。
_Avoid_：数组下标型持久引用、虚构子文档 ID、只能整段替换的列表

**精确选择器**：
以 `@短引用` 和逻辑节点定位某个原值的控制声明；Runtime 把公开句柄解析为内部文档身份，只解析明确位置，不做模糊迁移、语义查询或引用递归展开。
_Avoid_：敌意查询、相关性过滤器、字段猜测

**候选状态**：
一次游玩或修正在固定端点上提出的隔离世界文档结果；在权威提交成功前，它不是世界状态。
_Avoid_：待定世界、模型输出即事实、直接文件写入

### 提示词与模型会话

**主持预设**：
世界外可复用的语义提示框架，承载跨世界恒真的作者语义——通用文风、叙述人称、玩家代理权、裁判底线与通用状态维护判据；它进入游玩调用链的稳定 bootstrap，设定完善无法改写它。
_Avoid_：世界提示框架、模型配置、可执行工作流

**世界提示框架**：
内容包提供、创建世界时复制到世界控制面的内层提示框架；它只承载本世界特有的语义——题材边界、专属文风、本世界有哪些文档类型与结果写进哪一份、专属规则——以及哪些精确材料位置参与拼装，不重复通用判据，也不保存状态实际值。
_Avoid_：世界状态、通用状态维护判据、自由字符串模板

**提示词块**：
主持预设、世界提示框架或玩法预设中属于一个固定逻辑 role 的完整、可编辑 Markdown 语义块；Runtime 生成的工具与终态协议不属于提示词块。
_Avoid_：聊天消息、内部 DTO、任意模板片段

**逻辑 role**：
Runtime 用来稳定区分机械契约、作者指令、世界上下文、玩家输入、模型输出和工具结果的消息职责；provider adapter 只负责把它映射到真实协议 role。
_Avoid_：跨角色任意全序、全部塞进 user、provider 专属字符串

**提示词编译**：
把主持预设、世界提示框架、精确绑定和玩法工具定义确定性转换成 provider bootstrap 的过程；玩家原文随后作为普通玩家消息进入逻辑上下文。它不解释玩家意图，也不选择“相关”或“重要”材料。
_Avoid_：材料 DTO 序列化、Runtime 语义规划、JSON user 总包

**附加材料清单**：
某个世界端点保存的一组有界精确文档、节点或历史消息选择；全新上下文会把它编进 bootstrap，且它不是世界事实或自由提示词。既有端点中的 genesis 开场白引用是明确排除项。
_Avoid_：上下文焦点、语义查询、自动最近历史

**Prompt Preview**：
复用真实提示词编译与 provider 编码过程的只读检查结果；它展示稳定 bootstrap、紧随其后的首条玩家追加、provider role、材料来源、最终 Markdown、工具、Provider 配置、缓存边界，以及去除凭据后的真实 HTTP method、路径、header 名称和 body，但不调用模型或修改世界。
_Avoid_：近似预览、手写示例、调试 JSON 总包

**玩法预设**：
作者或玩家可复制、组合和分享的玩法包；游玩调用链开始时冻结当前选择，并从中取得工具全集、玩法扩展配置和稳定叙事规则。
_Avoid_：巨型提示词、世界控制、Runtime 插件

**模型会话**：
按顺序承载编译提示、玩家／模型消息、工具交换和续传项的一条逻辑工作上下文；其中尚未完整返回的模型片段不是世界权威，也不等同于 provider 会话 ID。
_Avoid_：世界、存档、权威历史、思维链

**Provider 续传载荷**：
Provider 完整响应中可以原样追加到下一次同协议请求的协议原生片段；Chat Completions 保存完整 assistant message，Responses 保存完整 `output` items，Anthropic Messages 保存完整 assistant `content` blocks。它不含 HTTP response 外壳，也不能从正文、返回推理、工具调用等解析投影重新组装；缺失、损坏或与冻结绑定不兼容时，本次模型会话必须失败并要求全新上下文。
_Avoid_：完整 HTTP response、解析后重建消息、从 text 切思维链、静默丢弃签名

**模型调用投影**：
Runtime 从 Provider 响应解析出的协议中立观察，包括可见正文、返回推理、工具调用、usage 和错误；它服务于工具执行、诊断与界面叙事，但不是下一次请求的续传来源。usage 逐项保留 input、uncached input、cache read、cache write、reasoning、output、total 及其 Provider／派生／未报告 provenance，未报告不能显示成零；返回推理只进入可展开诊断，不进入玩家正文、已提交叙事或世界 Authority。
_Avoid_：续传消息、隐藏思维推断、世界事实、把 reasoning 当正文

**模型调用进度**：
Runtime 对当前仍在执行的一次游玩生成给出的临时观察，按准备、等待首个 Provider 数据、接收返回推理／正文／工具参数、工具调用和后置请求区分阶段，并携带已运行时间、最近数据时间与累计数量。它只在收到 Provider 明示的 reasoning 增量后称为“思考中”；首个增量之前只能诚实表示排队、模型内部处理或网络等待尚不可区分。浏览器刷新可以重新读取同一 Runtime 进程拥有的进度，但它不是持久 Authority 或 Provider 续传载荷。
_Avoid_：隐藏思维探针、把无数据等同卡死、持久模型状态、客户端自造阶段

**模型绑定**：
一次模型会话固定使用的 provider endpoint、协议与方言、模型、续传 codec、Effort、Thinking 模式／手动预算、返回 thinking 内容、输出能力、工具策略和缓存策略；凭据与传输超时可以更新，但任何会改变请求或续传语义的字段都只能在全新上下文生效。它必须提供真实上下文窗口与最大输出能力，不能用脱离模型的 48k／128k 产品常量代替。
_Avoid_：自动猜测模型能力、会话中途换模、只比较模型名、固定全局 token 预算

### 游玩与权威

**AI 主持者**：
解释玩家行动、扮演非玩家角色、决定状态候选并生成可见叙事的语义决策者；它可以使用 Runtime 工具，但不能直接提交世界权威。
_Avoid_：Runtime、长期 NPC Agent、世界事实来源

**游玩调用链**：
从一次稳定 bootstrap 开始，按顺序承载玩家消息、完整或中断的 AI 响应、工具调用与工具结果的生产游玩上下文。AI 自己决定何时输出叙事、读取材料或更新世界；Runtime 不插入裁决／叙事阶段。玩家消息和每个已完整返回且含叙事或状态变化的模型响应分别推进权威，整条链没有集中结算。
_Avoid_：独立回合、连续会话、阶段流程、整链事务

**游玩推进**：
一条玩家消息或一份完整模型响应从输入被冻结，到可能推进 Authority，再到页面时间线能够稳定呈现的单次可恢复进展；进程中断后继续同一推进，不重新调用已经完整返回的模型结果。
_Avoid_：独立回合、整链事务、重新生成、模型响应本身

**全新上下文**：
点击同名按钮时从世界当前端点，在修改玩家消息时选择“作为全新上下文保存”则从该消息的逻辑父端点，重新读取 frame、状态、历史材料、主持预设和玩法预设，编译一份新的稳定 bootstrap，并用本次玩家原文或修改稿作为第一条 append。修改路径中的玩家原文已经由时间线修订提交，不能再次写入 Authority。它只重开模型工作上下文；玩家页面仍连续显示当前时间线上此前成立的已提交剧情和调用轨迹，并用分隔标记指出新上下文起点。
旧上下文不会进入新模型请求。普通全新上下文会把旧上下文的玩家输入、AI 正文、Provider 返回推理、工具调用、参数、结果、失败和 usage 全部保留为页面调用轨迹；修改路径只保留修改点以前的轨迹，原消息及其后续仍可由旧 Authority 端点恢复，但不进入当前页面时间线。
_Avoid_：清空世界、清空页面调用轨迹、把旧 conversation 发给模型、独立回合

**追加上下文**：
点击同名按钮后，已有上下文且输入非空时只追加玩家原文，输入为空时不制造 user 消息而直接触发续写；没有上下文且输入非空时自动按全新上下文开始。它是一次提交动作，不是需要保持的游玩模式。
_Avoid_：重新拼接历史、连续会话、自动摘要

**取消生成**：
玩家按当前 world、call chain 与 exchange 的精确身份请求 Runtime 停止这一轮。Runtime 把取消信号传到 Chat Completions、OpenAI Responses 或 Anthropic Messages 的实际 Provider 请求；已经提交的玩家原文和先前成立的 Authority 步骤保留，未完整结算的模型片段不进入 transcript、故事或 Authority。请求若已经派发则不能重放；若能证明取消发生在下一次派发之前，冻结请求仍可原样继续。浏览器流断开只是呈现故障，不等同玩家授权取消。
_Avoid_：回滚玩家原文、把取消当 Provider 故障、按旧 exchange 误取消、断线即取消

**原样重发模型请求**：
模型响应中断后，以空输入点击追加上下文，重新发送 Runtime 在派发前持久保存的上一份完整模型请求；不新增玩家消息，也不把中断片段放进模型上下文。它没有独立按钮或调用链事件。
_Avoid_：再次追加原玩家指令、从片段续写、换 operation 重放

**权威提交**：
Runtime 在固定父端点上原子接受的一笔不可变记录，保存本步精确叙事追加、变化后的完整世界文档和下一端点完整附加材料清单；玩家消息或一个完整模型响应都可以各自形成一笔，只有成功接受的提交才推进世界连续性。
_Avoid_：模型工具调用、YAML patch 日志、候选 overlay

**Runtime**：
唯一接受权威提交并维护操作结果、引用完整性、文件安全、物化和恢复的机械边界；它不判断敌意、重要性、秘密、修为强弱或剧情是否合理。
_Avoid_：AI 主持者、世界规则引擎、语义材料选择器

**玩家视图**：
世界控制用精确选择器指定、由 Runtime 从当前世界文档原样递归显示的只读界面；未选中不等于秘密，引用也不会自动展开。
_Avoid_：权限系统、玩家认知证明、通用安全投影

### 玩法扩展与界面产物

**玩法产物**：
游玩调用链产生并带有来源、频道和保留策略的具名界面输出；它不自动成为世界事实、已提交叙事或权威历史。
_Avoid_：世界状态、聊天消息、临时 DOM

**产物频道**：
把同类玩法产物映射到一个稳定界面用途的逻辑地址；频道声明追加、替换、按键更新、暂显或隐藏等呈现语义，而不决定世界权威。
_Avoid_：React 组件名、iframe 实例、历史消息角色

**活动产物投影**：
从已保存玩法产物及频道策略机械得出的当前可见集合；它可以重建或替换，不是玩法产物的唯一副本。
_Avoid_：产物历史、世界状态、浏览器缓存

**玩法扩展**：
把玩法产物或玩家视图转换为可交互呈现的一组正则、渲染器和脚本；第一方功能与作者扩展使用同一公开契约。
_Avoid_：Runtime 特判、模型工具、后端业务插件

**前端扩展宿主**：
在本地可信内容假设下运行玩法扩展并提供有限界面桥接的呈现边界；它可以处理 Markdown、HTML 与 JavaScript，但不能直接取得模型工具或权威提交能力。
_Avoid_：安全沙箱承诺、Runtime、世界规则引擎
