# V1 历史线性且不可改写

一个世界的全部权威提交形成不可改写的总顺序，当前状态由 genesis 与不可变文件提交确定性恢复；修正只能追加新提交，不能编辑、删除或移动既有历史头。玩家从旧端点重新开始时，Runtime 把来源 genesis 和截至所选端点的不可变提交完整复制到具有新身份的物理闭包世界，同时改写世界所有权身份并沿用派生时的当前控制；不能把恢复后的最终快照压成新 genesis，因为那会让仍然可见的祖先记录失去可恢复、可修改和可再次分支的本地端点。来源世界不回滚，派生世界也不保存对来源世界的运行依赖或共享可变状态。
